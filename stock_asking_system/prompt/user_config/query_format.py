#!/usr/bin/env python3
"""
股票问答系统 - 用户查询格式配置

📖 使用说明：
本文件包含发送给 AI 的用户查询格式模板和返回要求。
占位符说明：
- {query}: 用户查询内容
- {stock_count}: 股票池数量
- {lookback_days}: 回溯天数
- {top_n}: 返回数量
"""


# ==================== 用户查询格式要求 ====================

USER_QUERY_FORMAT = """请分析以下股票查询需求：

## 查询内容
{query}

## 可用数据
- 股票池：{stock_count} 只股票
- 数据时间范围：最近 {lookback_days} 个交易日
- 可用字段和工具：参考上述 SKILL 文档

## 执行步骤
1. **分析查询意图**：理解用户想要筛选什么类型的股票
2. **确定筛选条件**：列出需要的技术指标和筛选条件
3. **使用 MCP 工具**：调用工具计算指标和筛选股票
4. **计算置信度**：为每只股票计算 0-1 之间的置信度
5. **返回结果**：返回前 {top_n} 只股票，按置信度降序排序

## 返回格式（严格 JSON）

```json
{{
  "query_analysis": "查询意图的详细分析，说明理解的查询目标",
  "conditions": [
    "条件 1：具体的筛选条件描述",
    "条件 2：具体的筛选条件描述"
  ],
  "results": [
    {{
      "stock_code": "600519.SH",
      "stock_name": "贵州茅台",
      "confidence": 0.85,
      "reason": "具体的推荐理由，必须包含实际数据支撑（如：5 日涨幅 8.5%，成交量放大 2.3 倍，RSI=72 表示超买）",
      "metrics": {{
        "latest_price": 1850.50,
        "change_pct": 8.5,
        "volume_ratio": 2.3,
        "rsi": 72.0
      }}
    }}
  ]
}}
```

## 重要提醒
1. ⚠️ 只能使用 SKILL 文档中列出的字段和工具
2. ⚠️ 置信度必须基于实际数据计算，不能随意设定
3. ⚠️ 推荐理由必须具体，包含实际指标数值
4. ⚠️ 直接返回 JSON，不要有任何其他文字
5. ⚠️ 如果无法完成查询，在 query_analysis 中说明原因，results 返回空数组

请直接返回 JSON，不要有其他文字。"""
