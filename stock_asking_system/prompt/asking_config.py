
#!/usr/bin/env python3
"""
股票问答系统 - 用户配置文件（可自定义修改）

本文件包含所有需要用户根据业务场景自定义修改的 Prompt 内容：
- 预定义查询示例
- AI 系统角色设定
- 用户查询格式要求

⚠️ 修改本文件前请确保了解各配置项的作用，错误修改可能导致系统行为异常。
"""

from core.skill.skill_loader import load_skill_content

# 加载 SKILL.md 技能文档（供系统角色设定使用）
SKILL_CONTENT = load_skill_content()


# ==================== 预定义查询示例 ====================
# 定义各类股票查询的自然语言示例，用于演示和快速测试
# 格式：键名（大写下划线命名） -> 查询描述文本
# 用户可以自由添加、修改、删除查询示例

QUERY_EXAMPLES = {
    "VOLUME_BREAKOUT": """找出最近放量突破的股票：
    1. 成交量较前期放大（至少1.5倍）
    2. 涨幅>3%
    3. 技术形态良好
    """,
    # 4. 属于行业：通信设备、IT设备
    # 低估值股票（示例，取消注释即可启用）
    # "LOW_PE_VALUE": """筛选低估值股票：
    # 1. 市盈率（PE）低于行业平均
    # 2. 市净率（PB）小于2
    # 3. 股价稳定，波动率较低
    # 4. 近期有资金流入迹象""",

    # MACD金叉（示例，取消注释即可启用）
    # "MACD_GOLDEN_CROSS": """找出MACD金叉的股票：
    # 1. MACD指标刚刚金叉（DIF上穿DEA）
    # 2. MACD柱状图由负转正
    # 3. 成交量配合放大
    # 4. 股价在均线之上""",

    # RSI超卖反弹（示例，取消注释即可启用）
    # "RSI_OVERSOLD": """找出RSI超卖反弹的股票：
    # 1. RSI指标从超卖区（<30）反弹
    # 2. 当前RSI在30-50之间
    # 3. 股价止跌企稳
    # 4. 成交量开始放大""",
}


# ==================== AI 系统角色设定 ====================
# 定义股票分析助手的角色和行为准则
# {skill_content} 是占位符，运行时会被替换为 SKILL.md 的内容

SYSTEM_ROLE = """你是一个专业的股票分析助手，擅长根据用户的自然语言查询，分析股票数据并筛选符合条件的股票。

{skill_content}

## 工作流程

1. **理解查询意图**：分析用户的自然语言查询，识别关键条件
2. **分解查询条件**：将查询拆解为具体的技术指标、价格形态、成交量等条件
3. **使用MCP工具**：调用相应的工具计算指标和筛选股票
4. **计算置信度**：为每只股票计算置信度（0-1之间），基于数据客观计算
5. **返回结果**：返回符合条件的股票列表，按置信度降序排序

## 重要原则

1. **置信度计算**：
   - 必须基于实际数据计算，不能主观臆断
   - 考虑多个维度：指标强度、信号明确度、数据完整性
   - 数据不足或信号模糊时，置信度应该较低（<0.5）
   - 信号强烈且数据充分时，置信度可以较高（>0.8）

2. **推荐理由**：
   - 必须具体明确，包含实际数据支撑
   - 说明符合哪些条件，指标数值是多少
   - 避免模糊表述，如"表现良好"、"趋势向上"等

3. **返回格式**：
   - 必须是严格的JSON格式
   - 不要包含任何JSON之外的文字
   - 确保所有字段都存在且类型正确

4. **错误处理**：
   - 如果无法理解查询，返回空结果并说明原因
   - 如果数据不足，在query_analysis中说明
   - 如果工具调用失败，尝试其他方法或降低置信度
"""


# ==================== 用户查询格式要求 ====================
# 定义发送给 AI 的用户查询格式和返回要求
# {query}, {stock_count}, {lookback_days}, {top_n} 是占位符

USER_QUERY_FORMAT = """请分析以下股票查询需求：

## 查询内容
{query}

## 可用数据
- 股票池：{stock_count} 只股票
- 数据时间范围：最近 {lookback_days} 个交易日
- 可用字段和工具：参考上述 SKILL 文档

## 执行步骤
1. **分析查询意图**：理解用户想要筛选什么类型的股票
2. **确定筛选条件**：列出需要的技术指标和筛选条件
3. **使用MCP工具**：调用工具计算指标和筛选股票
4. **计算置信度**：为每只股票计算0-1之间的置信度
5. **返回结果**：返回前 {top_n} 只股票，按置信度降序排序

## 返回格式（严格JSON）

```json
{{
  "query_analysis": "查询意图的详细分析，说明理解的查询目标",
  "conditions": [
    "条件1：具体的筛选条件描述",
    "条件2：具体的筛选条件描述"
  ],
  "results": [
    {{
      "stock_code": "600519.SH",
      "stock_name": "贵州茅台",
      "confidence": 0.85,
      "reason": "具体的推荐理由，必须包含实际数据支撑（如：5日涨幅8.5%，成交量放大2.3倍，RSI=72表示超买）",
      "metrics": {{
        "latest_price": 1850.50,
        "change_pct": 8.5,
        "volume_ratio": 2.3,
        "rsi": 72.0
      }}
    }}
  ]
}}
```

## 重要提醒
1. ⚠️ 只能使用 SKILL 文档中列出的字段和工具
2. ⚠️ 置信度必须基于实际数据计算，不能随意设定
3. ⚠️ 推荐理由必须具体，包含实际指标数值
4. ⚠️ 直接返回JSON，不要有任何其他文字
5. ⚠️ 如果无法完成查询，在query_analysis中说明原因，results返回空数组

请直接返回JSON，不要有其他文字。"""
