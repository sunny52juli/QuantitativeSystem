#!/usr/bin/env python3
"""
因子回测系统 - 用户配置文件（可自定义修改）

本文件包含所有需要用户根据业务场景自定义修改的 Prompt 内容：
- 策略描述
- AI 系统角色设定
- 用户请求格式要求
- Fallback 技能文档
- 优化建议

⚠️ 修改本文件前请确保了解各配置项的作用，错误修改可能导致系统行为异常。
"""


# ==================== 策略描述配置 ====================
# 定义各类投资策略的自然语言描述，用于指导 AI 生成对应的因子
# 格式：键名（大写下划线命名） -> 策略描述文本
# 用户可以自由添加、修改、删除策略

STRATEGY_CONFIGS = {
    # 近日强势股票策略
    "RECENT_STRONG_STOCKS": """生成近日强势股票的因子，重点关注：
1. 短期动量（5-10日收益率）
2. 价格突破（相对高点位置）
3. 成交量配合（量价齐升）
4. 技术指标强势（RSI、MACD等）
5. 波动率特征（强势股的波动特性）""",

    # 价值股票策略（示例，取消注释即可启用）
    # "VALUE_STOCKS": """生成价值股票的因子，重点关注：
    # 1. 估值指标（PE、PB、PS等）
    # 2. 盈利能力（ROE、ROA等）
    # 3. 成长性（营收增长、利润增长）
    # 4. 财务健康度（负债率、流动比率）
    # 5. 股息率""",

    # 动量反转策略（示例，取消注释即可启用）
    # "MOMENTUM_REVERSAL": """生成动量反转因子，重点关注：
    # 1. 中长期动量（20-60日）
    # 2. 短期反转信号
    # 3. 波动率变化
    # 4. 成交量异常
    # 5. 技术形态突破""",
}


# ==================== AI 系统角色设定 ====================
# 定义 AI 因子挖掘器的角色和行为准则
# {skill_content} 是占位符，运行时会被替换为 SKILL.md 的内容

SYSTEM_ROLE = """你是量化因子专家，可以使用MCP工具构建复杂因子。

{skill_content}

重要原则:
1. 简单因子: 只用基础表达式，不用工具
2. 复杂因子: 组合工具 + 基础表达式
3. 工具步骤不超过3步
4. 返回严格JSON格式

⚠️ 数据字段约束（必须严格遵守）:
- 只能使用Skill中"可用数据字段"章节列出的字段
- 禁止编造或假设任何未列出的字段（如"换手率"、"市值"、"涨停价"等）
- 如需其他指标，必须通过标准字段计算得到
- 违反此约束将导致因子计算失败"""


# ==================== 用户请求格式要求 ====================
# 定义发送给 AI 的请求格式和返回要求
# {n_factors} 和 {strategy} 是占位符，运行时会被替换

USER_REQUEST_FORMAT = """请根据以下策略生成{n_factors}个量化因子：

策略描述：{strategy}

要求：
1. 返回严格的JSON格式
2. 每个因子包含：name（因子名称）、tools（工具步骤列表）、expression（最终表达式）、rationale（设计理由）
3. tools是一个列表，每个元素包含：tool（工具名）、params（参数字典）、var（变量名）
4. 简单因子可以不使用工具，直接用expression
5. 复杂因子可以组合多个工具，但不超过3步

⚠️ 重要规则：
- expression和params中引用变量时，必须使用tools中定义的var名称，不能使用工具名
- 例如：如果tool是pct_change，var是mom5，那么后续引用必须用mom5，不能用pct_change
- 如果需要计算量价相关性，必须先分别计算价格变化和成交量变化，存为不同的var
- params中的字段名必须是基础数据字段（如"收盘价"、"成交量"）或已定义的var名称

返回格式示例：
[
  {{
    "name": "量价动量因子",
    "tools": [
      {{"tool": "pct_change", "params": {{"values": "收盘价", "periods": 5}}, "var": "price_mom"}},
      {{"tool": "pct_change", "params": {{"values": "成交量", "periods": 5}}, "var": "vol_mom"}},
      {{"tool": "correlation", "params": {{"x": "price_mom", "y": "vol_mom", "window": 10}}, "var": "pv_corr"}}
    ],
    "expression": "pv_corr",
    "rationale": "价格动量与成交量动量的相关性，捕捉量价配合特征"
  }}
]

请直接返回JSON数组，不要有其他文字。"""


# ==================== Fallback 技能文档 ====================
# 当 SKILL.md 文件不存在时，使用此简化版技能文档

FALLBACK_SKILL_CONTENT = """# Factor Mining Skill - 简化版

## 可用基础字段
开盘价, 最高价, 最低价, 收盘价, 成交量, 成交额

## 可用工具
1. pct_change - 计算百分比变化
2. rolling_mean - 滚动平均
3. rolling_std - 滚动标准差
4. rank_normalize - 排名标准化
5. zscore_normalize - Z-score标准化

## 示例
```json
{
  "name": "短期动量",
  "tools": [
    {"tool": "pct_change", "params": {"values": "收盘价", "periods": 5}, "var": "mom5"}
  ],
  "expression": "mom5",
  "rationale": "5日价格动量"
}
```
"""


# ==================== 优化建议配置 ====================
# 因子回测后的优化建议模板，键是场景标识，值是建议文本

OPTIMIZATION_SUGGESTIONS = {
    'poor_return': "因子表现不佳，建议调整动量周期或加入反转逻辑",
    'low_return': "收益率偏低，可尝试结合其他技术指标增强信号",
    'good_return': "收益率表现良好，可考虑增加持仓权重",
    'low_sharpe': "风险调整后收益偏低，建议优化波动率控制",
    'high_drawdown': "最大回撤较大，建议加入止损机制或降低仓位",
    'complex_tools': "工具组合较复杂，可尝试简化以提高稳定性",
    'computation_error': "因子计算失败，建议检查表达式和工具参数",
}
